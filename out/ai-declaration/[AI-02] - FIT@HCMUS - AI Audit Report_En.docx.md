# Faculty of Information Technology (FIT) – Ho Chi Minh City University of Science (HCMUS)

## CS423 / CSC13003 – Software Testing (AI-augmented · 2026)

### AI POLICY · TEMPLATES — 2026 v1.0

# AI Audit Report — 5-section Template per Artifact

_Mandatory appendix for every AI-assisted homework (HW#01–HW#06, and Seminar)._

_Adapted from Med Kharbach, PhD (2026) — AI Use Policy Templates for Higher Education. CC BY-NC-SA 4.0. This adaptation is prepared for FIT@HCMUS – CS423 / CSC15003 Software Testing course._

---

## 1. Student Information

| Field                                   | Value              |
| --------------------------------------- | ------------------ |
| **Student name (printed):**             | Nguyen Bao Duy     |
| **Student ID:**                         | 23127179           |
| **Class / Cohort:**                     | 23KTPM2            |
| **Assignment ID (e.g., HW#00, HW#02):** | HW04               |
| **Assignment date:**                    | 28/07/2026         |
| **AI tool(s) used:**                    | Claude Code, Codex |
| **AI tool(s) used:**                    | [X] Yes [ ] No     |

---

## 2. Instructions (read before filling)

- Add one row per AI-generated artifact (test case, script, checklist, OpenAPI spec, JMeter plan, etc.).
- Paste the verbatim prompt — DO NOT paraphrase.
- Paste the verbatim AI output (or include a labelled screenshot in the report).
- Tag the verdict: VALID / INVALID / INCOMPLETE.
- Reasoning must cite a course slide, ISTQB section, or technical RFC.
- Show the corrected artifact with the change highlighted.
- Sample rows are in italic — replace them before submission.

---

## 3. Audit Table — one row per artifact

| (1) Prompt + Tool                                                                                                                         | (2) AI Output                                                                                      | (3) Verdict | (4) Reasoning (ISTQB)                                                                                          | (5) Student Fix                                                                                        |
| ----------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| **Sample _(italic)_ — replace before submission:**                                                                                        |                                                                                                    |             |                                                                                                                |                                                                                                        |
| Tool: AI Tool (e.g., ChatGPT, Claude, Gemini)<br>Time: 14:32 25/02/2026<br>Prompt: "Generate test cases for parsePhoneNumberVN function…" | TC01: parsePhoneNumberVN("0912345678")<br>Expected: {prefix:84, number:912345678, valid:true}<br>… | INCOMPLETE  | AI ignored RFC 3966 international format. ISTQB FL §4.3 Boundary Value Analysis requires testing format edges. | Added TC: parsePhoneNumberVN("+84-91-234-5678")<br>Expected: {prefix:84, number:912345678, valid:true} |
| **Artifact #1 — `docs/implementation-plan/implementation_plan.md`**<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 16:16 09/08/2026<br><br>Prompt (verbatim):<br>"Before we build the test codebase, I need you create implementation_plan.md.<br><br>In HW02, I use the idea of completing 1 feature before building SKILL to finish task => The submissions of other remain feature will be created by using this SKILL => Check the C:\Users\Duy\Desktop\automation-testing-hw04\references\hw2\eshop-sut-hw2-testing\out\docs => Can we resue this approach from HW02?" | **357-line plan** — too large to inline. **Verifiable public reference:** commit `50c10df` in the submission repo → `git show 50c10df:docs/implementation-plan/implementation_plan.md`. (Raw session transcript retained locally at `trash/claude-session1.md`, lines 1882–1984; that path is git-ignored and therefore *not* part of the submission — the commit SHA is the authoritative record.) Summary of what it produced:<br><br>• §1 inheritance table: HW02's *example-first / deliverable-first / freeze-before-execute / audit-as-you-go* carried over; HW02 "Model C" (no test-runner, no assertions-in-code) marked **SUPERSEDED** by HW04's Playwright mandate.<br>• §1.4 counted reusable HW02 cases: FR-04 = 16, FR-08 = **4**, FR-15 = 20 → flagged FR-08 as 8 short of the ≥12 minimum.<br>• §3 froze 5 architecture decisions (UI-first + API-assist, per-worker user isolation, selector policy, 3 assertion patterns, JSON data).<br>• 8 steps: pilot FR-04 by hand → extract skill → apply to FR-08/FR-15 → globals → video. | **INCOMPLETE** | Three defects found in student review.<br><br>**(a) Traceability failure — the main one.** Step 5.1 proposed FR-08 cases covering *coupon interaction*, *order status after checkout*, *quantity/stock edges* and *shipping-address validation*. Per the SUT spec these belong to **FR-09, FR-10 and FR-07** respectively — FR-08 states exactly 5 requirements. ISTQB CTFL v4.0 **§1.4.4 (Traceability between the Test Basis and Test Work Products)**: every test case must trace to the specific test-basis item it claims to cover; cases traced to the wrong FR give false coverage of FR-08 while the real requirements (total non-editable; ordered-product list displayed) had **zero** planned coverage.<br><br>**(b)** Commit rule stated only "Steps 2–6 touch `.spec.ts`" — not verifiable against HW04 §12's 8-commit minimum, and it silently counted Step 4 (which touches `SKILL.md` only).<br><br>**(c)** HTML-report evidence relied on reporter *metadata*; HW04 §11 treats the report as **TA-verified anti-cheat evidence**, so the stamp must be visible, not buried. | **(a)** Rewrote Step 5.1 around an explicit **R1–R5 requirement table** quoted from FR-08, added an out-of-scope list naming FR-07/FR-09/FR-10, and 10 candidate cases each tagged with its R-ref — prioritising **R2 and R3** (zero existing coverage). Added a **setup-vs-counted-case rule**: cart seeding (FR-07) and login (FR-02) are non-counting fixtures. Added a stop condition: a case not traceable to R1–R5 is dropped or demoted.<br><br>**(b)** Added **§5 Commit schedule**: 3 qualifying commits per feature (freeze A, freeze B, post-run fix) + 1 smoke = **floor of 10**; Step 4 explicitly marked **0 qualifying**; §5.3 forbids mixing spec and doc changes; verified by `git log --oneline -- '*.spec.ts' \| wc -l`.<br><br>**(c)** Added **§3.6**: 3 evidence layers with the reporter **`title`** as primary, verified against Playwright 1.62.1 source (`process.env.PLAYWRIGHT_HTML_TITLE \|\| options.title`) plus a mandatory `grep` check that the `<title>` is not the default. |
| **Artifact #2 — Step 1 implementation** (`playwright.config.ts` reporter title, `fixtures/base.ts` isolated-user fixture, `utils/api.ts`, `automation-architecture.md`)<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 16:45–17:05 09/08/2026<br><br>Prompt (verbatim, complete):<br>"Read:<br>- `docs/implementation-plan/implementation_plan.md`<br>- `docs/implementation-plan/automation-architecture.md`<br>- `out/ai-declaration/[AI-02] - FIT@HCMUS - AI Audit Report_En.docx.md`<br><br>Fix only these consistency issues first:<br><br>1. In `implementation_plan.md`, update Status:<br>- Mark Step 1 as complete.<br>- Change `NEXT ACTION` from Step 1 to Step 2.<br>- Remove the stale instruction that says to verify with the §3.6 grep. The current correct check is `cd automation && npm run verify:report`.<br><br>2. In the AI Audit row for Artifact #2, it currently says 'Prompt (verbatim, abridged...)'. That is internally inconsistent with the audit template's 'verbatim prompt' rule.<br>- Either paste the full prompt verbatim, or change the label so it does not claim 'verbatim'.<br>- Prefer pasting the full prompt if practical.<br><br>Commit and push this housekeeping change separately. It is non-qualifying."<br><br>_(Note: the row's original prompt cell abridged the Step 1 instruction, which contradicted the template's §2 rule "Paste the verbatim prompt — DO NOT paraphrase." The Step 1 prompt is reproduced in full below; the housekeeping prompt that corrected it is quoted above.)_<br><br>**Step 1 prompt (verbatim, complete):**<br>"Proceed with Step 1 from `docs/implementation-plan/implementation_plan.md`.<br><br>Before implementation, fix two minor stale wording issues in the plan:<br>1. In the folder structure comment, change 'the five frozen decisions' to 'the six frozen decisions'.<br>2. In §5.4, change 'Per §5 of Step 1' to a clearer reference such as 'Per Step 1 / §5.4'.<br><br>Then execute Step 1 only:<br><br>1. Add the Playwright HTML reporter `title` in `automation/playwright.config.ts`.<br>- It must visibly include `Run by: 23127179`<br>- It must include an ISO timestamp generated at config load<br>- Keep existing metadata and per-test annotation layers<br><br>2. Write `docs/implementation-plan/automation-architecture.md`.<br>- Freeze §3.1–§3.6 from the plan<br>- Include concise rationale and evidence<br>- Include the isolation verification output after the run<br><br>3. Implement the worker-scoped isolated-user fixture in `automation/fixtures/base.ts`.<br>- Register a unique user per worker via `POST /api/register`<br>- Login via API and expose `user` + `token`<br>- Preserve the existing `runBy` auto annotation<br>- Do not depend on seeded `test@eshop.com`<br><br>4. Add thin helper(s) in `automation/utils/` only if needed.<br>- Keep them small<br>- Do not create a full Page Object Model<br>- Do not modify anything under `eshop-sut/`<br><br>5. Prove isolation with a temporary Playwright spec:<br>- Create `automation/tests/_isolation-check.spec.ts`<br>- Run it on Chromium/Firefox/WebKit with parallel workers<br>- Run it twice<br>- Paste the relevant passing command output into `automation-architecture.md`<br>- Delete `_isolation-check.spec.ts` before committing<br>- Verify `git status` has no temporary spec<br><br>6. Verify the HTML report title:<br>- Run the §3.6 grep against `automation/playwright-report/index.html`<br>- Confirm the title is not the default `Playwright Test Report`<br><br>7. Update the AI Audit Report with a new row for this Step 1 Claude-generated artifact/code/doc work.<br>- Include my prompt verbatim<br>- Summarize the output if too large<br>- Mark the verdict based on your human review<br>- Update running totals<br><br>Keep the Step 1 commit non-qualifying: no `.spec.ts` file should be committed.<br><br>Show:<br>- `git diff --stat`<br>- relevant diff snippets<br>- test commands run and results<br>- `git status --short`<br>Then commit and push with message:<br>`Step 1: freeze automation architecture and isolated-user fixture`" | Produced, in order:<br><br>• `playwright.config.ts` — added `title: \`EShop Automation — Run by: 23127179 — ${ISO}\`` to the html reporter, ISO generated once at config load; kept `metadata` + the `runBy` auto-annotation.<br>• `utils/api.ts` — `registerAndLogin()` creating `hw04-{label}-{ts}-{rand}@eshop.test`.<br>• `fixtures/base.ts` — worker-scoped `api` + `isolatedUser` fixtures; `runBy` preserved.<br>• `tests/_isolation-check.spec.ts` — temporary; wrote a worker-unique phone, waited 750 ms, read back, asserted no cross-worker leak. **6/6 passed** over 2 runs × 3 browsers (ids 12/13/14 then 15/16/17).<br>• `scripts/verify-report-stamp.js` + `automation-architecture.md`.<br><br>Full diff: Step 1 commit on `main`. | **INCOMPLETE** | The delivered code was correct on first execution, but **the verification step prescribed by the plan was not** — and it is the AI's own earlier work, added as the fix for Artifact #1's finding (c). Step 6 of the prompt instructed a `grep` on `<title>` in `playwright-report/index.html`. Run as instructed it printed `<title>Playwright Test Report</title>` → **"FAIL: still default title"**, on a report whose title was in fact set correctly.<br><br>ISTQB CTFL v4.0 **§1.2.3 (Errors, Defects, Failures — false-positive / false-fail results)**: a check that reports failure when no defect exists is itself a defect in the test. Root cause: `index.html`'s `<title>` is a **static Vite shell tag** that always reads the default; the real title lives at `report.json → options.title` inside a **base64-encoded zip embedded** in the same file, and the page sets `document.title` from it at runtime. A plain-text grep cannot read compressed data, so the check could **never** observe the value it claimed to test. Acting on that false fail would have meant reverting a working feature. | Investigated instead of reverting. Decoded the embedded zip and found the value at `report.json.options.title`; confirmed the config option worked all along, and that the earlier claim "`PLAYWRIGHT_HTML_TITLE` does not work" was the *same* false signal (re-tested: the env var populates the field too).<br><br>Replaced the grep with **`automation/scripts/verify-report-stamp.js`** (`npm run verify:report`), which decodes the payload and asserts 5 conditions, exiting non-zero on failure — reused by Steps 2/3/5/6. Confirmed visually by rendering the report in Chromium: the stamp is the page `<h1>` **and** the tab title (screenshot: `docs/implementation-plan/evidence/report-run-by-2026-08-09.png`).<br><br>Corrected §3.6 of the plan (with the false-fail documented so it is not reintroduced) and every downstream reference to the old grep. Also hardened `utils/api.ts` after reading the SUT schema: `users.email` has **no UNIQUE constraint** and `POST /api/login` uses `db.get` (first match wins), so a duplicate email would hand two workers one account and break isolation *silently* — hence the random suffix. |
| **Artifact #3 — Step 2 FR-04 smoke spec** (`tests/fr-04-profile/phone-boundary.spec.ts`, `data/fr-04-profile.json`, `utils/session.ts`)<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 17:05–17:20 09/08/2026<br><br>Prompt (verbatim, complete — Step 2 portion; the housekeeping portion is logged with Artifact #2):<br>"Then proceed with Step 2 only: FR-04 vertical smoke.<br><br>Step 2 requirements:<br>1. Pick one FR-04 HW02 case with a clear oracle. Recommended: `TC-04-BVA-002` UI-path valid phone `0912345678` should be accepted per FR-04, because it exercises the real browser UI and is likely to expose the known frontend regex defect. If you choose a different case, justify why it is better for the vertical smoke.<br><br>2. Externalize the case data to `automation/data/fr-04-profile.json`. No inline test-data arrays/objects in the spec.<br><br>3. Drive the AI step-by-step to generate the Playwright spec.<br>- Import `{ test, expect }` from `../../fixtures/base`, not from `@playwright/test`.<br>- Use `isolatedUser`; do not use seeded `test@eshop.com`.<br>- Use the existing fixture/helper architecture from Step 1.<br>- Do not modify `eshop-sut/`.<br><br>4. Human-review the generated script before running. Check for: fragile selectors, wrong oracle, weak/missing assertions, flaky waits, accidental assertion weakening to match actual behavior.<br><br>5. Commit the smoke `.spec.ts` before its first run. Commit message: `freeze: FR-04 smoke spec`. This is the first qualifying §12 commit.<br><br>6. Run the smoke on all 3 browsers. If it fails because the product violates the FR-04 spec, do not weaken the assertion. Classify the failure through the real-defect gate. If confirmed, create/update the FR-04 automation report and bug report evidence as Step 2 output.<br><br>7. Run: `cd automation && npm run verify:report`; `cd automation && npx tsc --noEmit`<br><br>8. Update AI Audit with a new row for the Step 2 AI-generated smoke spec. Include the prompt/output or a clear artifact reference if too large, verdict, reasoning, and student fixes.<br><br>Show: selected HW02 case and oracle; generated/edited files; `git diff --stat`; the qualifying commit SHA; browser run result; `npm run verify:report` result; `npx tsc --noEmit` result; `git log --oneline -- '*.spec.ts' \| wc -l`<br><br>Do not start Step 3." | Generated `TC-04-BVA-002-UI`: data externalized to `data/fr-04-profile.json` (oracle text stored beside the expected values); spec importing `{ test, expect }` from `../../fixtures/base`, using the worker-scoped `isolatedUser`; three assertion patterns (UI-state dialog check, `waitForResponse` on `PUT /api/users/me`, independent `GET` round-trip) written as `expect.soft` so one run reports every violation; `utils/session.ts` seeding the auth token.<br><br>Frozen at **`e6cd87f`** before first execution. Run result: **3/3 browsers failed**, all four soft assertions, identically.<br><br>Full artefacts: `out/reports/FR-04-personal-profile/automation/report.md` (§6 holds the complete review table). | **INCOMPLETE** | Four defects found in review **before** the freeze commit — none in the oracle, all in how the script observed the SUT:<br><br>**(a) `getByLabel` is unusable here.** The obvious locator for the field labelled *"Số điện thoại"* fails: `Profile.jsx:137` renders `<label>` as a **sibling** of `<input>` with no `for`/`id`, `aria-label`, or nesting, so nothing associates them. ISTQB CTFL v4.0 **§6.2.1 (test automation — maintainability of test scripts)**: locators must bind to stable, intended identifiers; a locator chosen from *rendered appearance* rather than the DOM contract is fragile by construction.<br><br>**(b) `waitForResponse` would have masked the defect.** When client-side validation blocks, **no request is ever sent**, so awaiting the PUT yields an opaque timeout instead of the real finding. The AI wrote the happy path and did not reason that the failing branch changes the *shape* of the observation.<br><br>**(c)** `shippingAddress` was declared in the data file but never used by the spec — data and behaviour silently diverging.<br><br>**(d)** Typecheck failure: `addInitScript` callbacks are browser code referencing `window`/`localStorage`, but `tsconfig.lib` lacked `DOM`. | **(a)** Switched to `getByPlaceholder('VD: 0912345678')` — unique and still semantic, not a CSS/structural hack (architecture §3.3 order) — and recorded *why* the label path is unavailable.<br><br>**(b)** Captured the response as a value (`.catch(() => null)`) and asserted on that value, so "no PUT was issued" surfaces as a specific readable failure rather than a timeout. This is what made the full impact chain visible: rejection → no request → nothing persisted.<br><br>**(c)** Spec now fills the address, so `phone` is genuinely the only spec-relevant variable.<br><br>**(d)** Added `DOM` to `tsconfig.lib`; `npx tsc --noEmit` exits 0. Caught before the freeze.<br><br>**Post-run: nothing was changed.** All 3 browsers failed; the real-defect gate confirmed a genuine product defect (`Profile.jsx:43` uses `/^[1-9][0-9]{8,9}$/`, whose `[1-9]` is the inverse of the spec's *"bắt đầu bằng số 0"*), cross-checked against the backend, which accepts and persists the same value — so the fault is frontend-only. Filed as `BUG-04-101`. **No assertion was weakened**; the red test is the evidence. |
| **Artifact #4 — Step 3 Batch A specs + execution analysis** (`tests/fr-04-profile/phone-boundary-ui.spec.ts`, `data/fr-04-profile.json`, `fixtures/base.ts` `freshUser`)<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 17:30–18:00 09/08/2026<br><br>Prompt (verbatim, complete — the Batch A generation portion; the execution prompt follows):<br>"4. Select the FR-04 full-pilot case set from HW02. … Select at least 12 total FR-04 cases. Include the already automated `TC-04-BVA-002-UI` as covered. Avoid duplicating the same assertion surface unless it adds clear value. Map each selected case to UI / `page.route()` / `APIRequestContext`. List any non-automatable case with reason.<br>5. Externalize or extend FR-04 data in `automation/data/fr-04-profile.json`. No inline test-data arrays/objects in specs. Preserve oracle text and expected sources.<br>6. Generate and human-review only Batch A specs for Step 3. Target about 4 cases. Import `{ test, expect }` from `../../fixtures/base`. Use `isolatedUser`; never use seeded `test@eshop.com`. Reuse `seedSession`. Do not weaken assertions for BUG-04-101 or other known defects. Record AI misses and fixes in `out/reports/FR-04-personal-profile/automation/report.md`.<br>7. Commit Batch A before running it. Commit message: `freeze: FR-04 specs batch A`.<br>8. Stop immediately after that freeze commit. Do not run Batch A yet. Do not start Batch B."<br><br>**Execution prompt (verbatim, complete):** "Review current repo state first, then continue Step 3 by running only FR-04 Batch A. … Do not modify `.spec.ts` before the run. Do not start Batch B yet. Expected prediction already recorded: BVA-001 and BVA-004 should pass. BVA-003 should fail as another BUG-04-101 manifestation. BVA-005 should fail in the opposite direction because the UI accepts a spec-invalid leading-1 phone. Do not weaken assertions. Apply the real-defect gate. Tasks: 1. Run only `automation/tests/fr-04-profile/phone-boundary-ui.spec.ts` across all 3 browsers. 2. Record actual results per case and browser. 3. Compare actual results against the pre-run prediction … 4. If failures are the same root cause as BUG-04-101, update the existing bug report and GitHub issue #1 with Batch A evidence instead of creating a duplicate issue. 5. If a failure has a distinct root cause, create a new bug report + GitHub issue with screenshot evidence. 6. Copy the generated HTML report … 7. Run: `npm run verify:report`; verifier against the copied report if the script supports a path; `npx tsc --noEmit`; `git log --oneline -- '*.spec.ts' \| wc -l`. 8. Update the AI audit … 9. Commit only report/evidence/status updates as a non-qualifying output commit. … Stop after Batch A output is committed." | 4 data-driven UI boundary cases completing the 5-point set, frozen at **`8053add`** before execution, plus a test-scoped `freshUser` fixture.<br><br>Execution took **4 runs**: run 1 = 7 failed/5 passed, run 2 = 8 failed/4 passed, runs 3–4 = **6 failed/6 passed, identical, zero timeouts**.<br><br>Final: `BVA-001` ✅, `BVA-003` ❌, `BVA-004` ✅, `BVA-005` ❌ — **4/4 matching the pre-run prediction**. Confirmed `BUG-04-101` (widened) and a new `BUG-04-102`.<br><br>Full analysis: `out/reports/FR-04-personal-profile/automation/report.md` §10–11. | **INCOMPLETE** | Five defects found in review before the freeze (report §10) and two more surfaced by execution (§11.6). The two most significant:<br><br>**(a) An invented oracle.** For "invalid" cases the AI's natural assertion was `expect(phone).toBeNull()`. HW02's own note states a *"not persisted as X"* expectation does **not** prescribe how the SUT avoids storing it — reject, coerce, and truncate are all spec-compliant. ISTQB CTFL v4.0 **§1.4.4 (traceability between test basis and testware)**: an expected result must trace to the test basis, and `toBeNull()` asserts a behaviour line 65 never states. It would have produced a **false failure** on a compliant SUT.<br><br>**(b) A flaky wait that produced a false product defect.** `page.goto` used the default `waitUntil: 'load'`, which blocks on every subresource of a **Vite dev server**. Under 3-browser parallel load this timed out at 30s **before any assertion ran**, failing `TC-04-BVA-001-UI` on Firefox — a case predicted to pass. Taken at face value it would have been written up as a Firefox-specific product defect **that does not exist**. Compounding it, Playwright's default `cpus/2` = **6 workers** over-subscribed a single Vite server + single-threaded SQLite backend, inflating 2–13s tests to 30–52s. | **(a)** Modelled persistence in the data file as `{ mode: "equals" \| "notEquals", value }` and branched on it, so the spec asserts exactly what the oracle states and nothing more.<br><br>**(b)** Diagnosed rather than accepted: the case **passed in isolation on Firefox (12.9s)** and passed under load on the other two browsers, and the error was at navigation — so no assertion had run. Classified **test-side** and fixed in two parts — `waitUntil: 'domcontentloaded'` (letting the retrying element assertion be the real readiness signal) and `workers: 3` (one per browser project). Runs 3 and 4 then produced identical results with zero timeouts. Committed separately as `fix: FR-04 post-run corrections` with the reasoning, per plan §5.2.<br><br>Also: added a test-scoped `freshUser` fixture (the worker-scoped account is shared by 4 mutating tests, and `BVA-005` leaves a value stored); added a batch-size guard so a silent data-file edit cannot shrink the suite into a false green; recorded that `BVA-001`/`BVA-004` pass **incidentally** — the regex never enforces the length rule, so green there must not be read as length validation working.<br><br>**No assertion, expected value or oracle was changed at any point.** `BVA-005`'s persistence failure was traced to a **distinct** root cause (`server.js:118-135` has no validation, survives any frontend fix) and filed as `BUG-04-102` rather than folded into `BUG-04-101`. |
| **Artifact #5 — Step 3 Batch B specs + execution analysis** (`tests/fr-04-profile/phone-boundary-api.spec.ts`, Batch B data in `data/fr-04-profile.json`)<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 18:05–18:30 09/08/2026<br><br>Prompt (verbatim, complete — generation): "Review current repo state first, then continue with FR-04 Step 3 Batch B freeze only. … Then execute Batch B freeze only: 1. Read: `out/reports/FR-04-personal-profile/automation/report.md`; `automation/data/fr-04-profile.json`; current FR-04 bug report; HW02 FR-04 BVA source reports; `eshop-sut/README.md` FR-04; `eshop-sut/api_specification.md` `PUT /api/users/me`. 2. Implement Batch B cases only: `TC-04-BVA-006-API` … `TC-04-BVA-010-API`. 3. Use `APIRequestContext`, not UI, because these assert backend persistence/validation behavior directly. 4. Externalize all new case data in `automation/data/fr-04-profile.json`; no inline test-data arrays/objects in specs. 5. Preserve oracle discipline: expected values come from FR-04 line 65 / HW02 accepted oracle only. do not derive expectations from current SUT behavior. invalid phone cases should assert 'not persisted as invalid value,' not a made-up exact alternative like null/empty unless the oracle says so. 6. Human-review the generated Batch B spec before committing. Specifically check: no use of seeded `test@eshop.com`; fresh user per test where profile state is asserted; no assertion weakening because BUG-04-102 already exists; no duplicate coverage that merely restates Batch A UI behavior; clear failure messages linking to FR-04 line 65. 7. Commit Batch B before running it: commit message `freeze: FR-04 specs batch B` … 8. Stop immediately after the Batch B freeze commit."<br><br>**Execution prompt (verbatim, complete):** "Review current repo state first, then continue by running FR-04 Batch B only. … Do not modify `.spec.ts` before the run. Do not start Batch C yet. Expected prediction already recorded: BVA-007 and BVA-008 pass. BVA-006, BVA-009, BVA-010 fail as BUG-04-102. Expected tally: 2 pass / 3 fail per project, 6 pass / 9 fail total. API-only cases do not launch a browser. Be honest in the report: they run once per Playwright project for matrix uniformity, but they are not browser-coverage evidence. Tasks: 1. First fix resume-state housekeeping … 2. Run only: `cd automation && npx playwright test tests/fr-04-profile/phone-boundary-api.spec.ts` 3. Record actual results per case/project … Classify every failure through the real-defect gate. Do not weaken assertions. If failures are BUG-04-102, update existing bug report and GitHub issue #2 instead of duplicating. Create a new bug only for a distinct root cause. 4. Copy the generated HTML report … 5. Run verification … 6. Update AI audit … 7. Commit Batch B output as non-qualifying … Stop after Batch B output is committed." | 5 data-driven API cases frozen at **`5af1749`** before execution, using `APIRequestContext` + the test-scoped `freshUser` fixture.<br><br>Run result: **6 passed / 9 failed** in a single invocation, **zero timeouts**, 3.5s total — matching the pre-run prediction **5/5**. All 9 failures confirmed as `BUG-04-102`; issue #2 updated, no duplicate filed.<br><br>Full analysis: `out/reports/FR-04-personal-profile/automation/report.md` §12–13. | **INCOMPLETE** | Five defects found in review **before** the freeze (report §12); none surfaced during execution. The two that mattered most:<br><br>**(a) A missing comma left the shared data file unparseable.** Appending the Batch B cases left the previous array element unterminated. All three spec files import `fr-04-profile.json`, so the **entire suite** would have failed to load rather than reporting results — a disproportionate blast radius for a one-character slip.<br><br>**(b) Asserting a status code for invalid input would have invented an oracle.** The symmetric generation (`toBe(200)` valid / `toBe(400)` invalid) reads rigorous, but `api_specification.md` §2.2 documents **only the request body** for `PUT /api/users/me` — no validation rule, no error-response contract — and README line 65 never states *how* a refusal must occur. ISTQB CTFL v4.0 **§1.4.4 (traceability between test basis and testware)**: half of that matched pair had no source in the test basis, and it would fail a compliant implementation that chose to coerce rather than reject. | **(a)** Caught by parsing the file with `node -e` **before** committing; that parse-and-summarise check is now the routine gate after any data edit.<br><br>**(b)** Status asserted **only** for spec-valid cases (traceable to HW02's "must succeed"); for invalid cases the status is recorded as a test **annotation** — evidence without an unfounded claim. The oracle stays exactly "not stored as this value".<br><br>Also, pre-freeze: justified `TC-04-BVA-010-API` rather than assuming it (Batch A's `BVA-005-UI` proves a *compound* "UI accepts and backend stores"; the API case **isolates the backend half**, which is what substantiates `BUG-04-102` surviving a frontend fix — and the run vindicated this: `BVA-006`/`BVA-009` fail on inputs the frontend already rejects); added an email read-back assertion so a mis-targeted write is reported rather than silently corrupting the phone assertion; and recorded that these cases **launch no browser**, so their 15 executions are matrix uniformity and are excluded from the §6 browser-run count rather than inflating it.<br><br>Post-run: **no `.spec.ts` change was needed** — no `fix:` commit for Batch B. Failures were confirmed against the SUT independently of Playwright (200 + verbatim persistence for all five values), classified as one existing root cause, and **no assertion was weakened**. |
| **Artifact #6 — Step 3 Batch C specs + execution analysis** (`tests/fr-04-profile/profile-fields.spec.ts`, Batch C data in `data/fr-04-profile.json`)<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 18:35–19:10 09/08/2026<br><br>Prompt (verbatim, complete — generation): "Review current repo state first, then continue with FR-04 Step 3 Batch C freeze only. … 2. Implement Batch C's 6 EP cases only: `TC-04-EP-001-API`, `TC-04-EP-002-API`, `TC-04-EP-003-API`, `TC-04-EP-004-API`, `TC-04-EP-005-API`, `TC-04-EP-006-UI-API`. 3. Externalize all new case data in `automation/data/fr-04-profile.json`. No inline test-data arrays/objects in specs. `EP-002` and `EP-003` must cite HW02 accepted assumptions A2/A3 in `expectedSource`. Do not invent expected behavior not supported by README, API spec, or HW02 accepted assumptions. 4. Use the correct surface per case: APIRequestContext for backend/persistence/forged payload cases. `TC-04-EP-006` is dual-surface: UI assertion: email field is disabled, covering README line 66 'qua giao diện'. API assertion: forged email is ignored/not persisted, covering the underlying immutability guarantee. 5. Human-review before freezing. Specifically check: no seeded `test@eshop.com`; use `freshUser` where profile state is asserted; no `isolatedUser` for mutating profile-state cases; no invented `400`/`null`/empty-string oracle unless the test basis explicitly says so; no assertion weakening for known BUG-04-101 or BUG-04-102; UI locators avoid `getByLabel` on the profile form because labels are not programmatically associated; API-only cases are documented as not browser-coverage evidence. 6. Run static gates only, not the tests … 7. Commit Batch C before running it: commit message `freeze: FR-04 specs batch C` … 8. Stop immediately after the Batch C freeze commit."<br><br>**Execution prompt (verbatim, complete):** "Review current repo state first, then run FR-04 Batch C only. … Then run only: `cd automation && npx playwright test tests/fr-04-profile/profile-fields.spec.ts`. Expected prediction already recorded: EP-001 pass; EP-002 fail; EP-003 pass; EP-004 fail as BUG-04-102 / convergence with BVA-010; EP-005 fail if `role` injection is applied; if confirmed, this is likely a new privilege-escalation defect, distinct from BUG-04-101 and BUG-04-102; EP-006 pass on both UI-disabled and forged-email API surfaces; Expected tally: 3 pass / 3 fail per project, 9 pass / 9 fail total. Rules: Do not modify `.spec.ts` before the run. Do not weaken assertions. Do not start the full FR-04 combined run yet. If a failure is a known root cause, update the existing issue/report instead of duplicating. If EP-005 exposes role escalation, create a new bug report + GitHub issue with evidence and classify it through the real-defect gate. If any failure is test-side, make a separate optional `fix: FR-04 post-run corrections` commit and explain why it is test-side. After the run: 1. Record actual Batch C results per case/project … 2. Compare prediction vs actual. 3. Update … bug-reports/report.md and GitHub issues as needed. 4. Copy the HTML report to … `batch-c.html`. 5. Do not overwrite `html-report/index.html` yet … 6. Run: `npm run verify:report`; verifier on copied `batch-c.html`; verifier on existing `index.html`, `batch-a.html`, `batch-b.html`; `npx tsc --noEmit`; `git log --oneline -- '*.spec.ts'`. 7. Update AI Audit Artifact #6 … 8. Commit Batch C output as non-qualifying if no spec changes are needed. Stop after Batch C output is committed." | 6 EP cases, each its own `test` block reading its own frozen case (they assert different things, unlike the A/B loops). Frozen at **`6942a0a`** before execution; static gates only at freeze time (data parses = 16 cases, `tsc` 0, `--list` = 18 tests).<br><br>Run: **9 passed / 9 failed** in one invocation, zero timeouts, 11.8s — matching the pre-run prediction **6/6**. Produced one **new Critical** defect (`BUG-04-103`, issue #3), widened `BUG-04-102`, and confirmed one by-design convergence.<br><br>Full analysis: `out/reports/FR-04-personal-profile/automation/report.md` §14–15. | **INCOMPLETE** | Five defects found in review **before** the freeze (report §14); none surfaced during execution. The two most consequential:<br><br>**(a) `TC-04-EP-004-API` duplicates `TC-04-BVA-010-API` outright** — same input, same assertion, same mechanism, **zero new coverage**. Not an AI invention: the overlap is inherited from **HW02's own design**, where the EP report designed one *representative* invalid-phone case and the BVA report expanded the full boundary set. In HW02 they sat in separate documents, so the convergence was invisible until both became runnable tests in one suite. ISTQB CTFL v4.0 **§1.4.4 (traceability)** cuts both ways here: dropping a frozen case breaks traceability to the test basis, but presenting a duplicate as independent evidence overstates coverage.<br><br>**(b) `EP-002` invited an invented oracle in a subtler form than findings 7/15.** The natural assertion for "empty `name` is invalid" is HTTP `400` or `name === null`. Accepted assumption **A2 grants neither** — it establishes the empty string as an invalid *class*, and HW02 deliberately froze the expectation as an **outcome** because the SUT documents no error-response contract. The recurring trap: "invalid" reads like "must be refused *somehow specific*". | **(a)** Kept for traceability but the convergence is **declared**, not hidden — `convergesWith` + `convergenceNote` in the data file and a `Converges with` annotation surfaced in the HTML report, so a reader cannot mistake it for independent evidence. The run bore this out: `EP-004` failed identically to `BVA-010-API` and was attributed to `BUG-04-102` without opening anything new.<br><br>**(b)** No status assertion at all; the sole oracle is `name` not persisted as `""`, with the failure message naming **A2** as its source. When it failed, the finding was recorded against `BUG-04-102` with an explicit **evidence-strength caveat** — A2 is `confidence: MED`, weaker than the phone findings' direct README line 65 citation, and the report says so rather than presenting them as equally grounded.<br><br>Also pre-freeze: added a **hard precondition** to `EP-005` asserting the account starts as `role: "user"` (finding 22) — which is exactly what let the resulting failure be attributed to the SUT with confidence rather than to a fixture handing over an elevated account; and used XPath from the label text for `EP-006`'s UI half, since `getByLabel` is unusable on this form and an index-based locator would break on field reordering.<br><br>**Post-run: no `.spec.ts` change was needed** — no `fix:` commit for Batch C. `EP-005`'s failure was put through the real-defect gate and **verified exploitable end to end outside Playwright** (self-update to `role: admin`, then re-login mints a genuinely signed **admin JWT**), classified as a **distinct root cause** from `BUG-04-102` (authorisation boundary vs format validation) and filed as **`BUG-04-103` / issue #3, Critical**. **No assertion was weakened.** |
| **Artifact #7 — Combined FR-04 run: feature-level report, `out/README.md` summary, HTML-report reorganisation**<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 19:50–20:15 09/08/2026<br><br>Prompt (verbatim, complete): "Review current repo state first, then run the combined FR-04 suite only. Before running, fix resume-state housekeeping in `docs/implementation-plan/implementation_plan.md` … Then run the full FR-04 suite: `cd automation && npx playwright test tests/fr-04-profile`. Expected combined result from prior frozen batch runs: 16 cases × 3 projects = 48 executions; expected per project: 7 pass / 9 fail; expected total: 21 pass / 27 fail; known defects: BUG-04-101: frontend phone regex; BUG-04-102: missing input validation in `PUT /api/users/me`; BUG-04-103: role privilege escalation; no new root cause is expected unless the combined run exposes interaction/flakiness. Rules: Do not modify `.spec.ts` before the run. Do not weaken assertions. Do not start Step 4 / skill extraction yet. If failures match existing root causes, update reports/issues only as combined-run evidence. If any new failure appears, classify through the real-defect gate. If any failure is test-side, make a separate optional `fix: FR-04 post-run corrections` commit and explain why it is test-side. HTML report handling: 1. Preserve the current `out/reports/FR-04-personal-profile/html-report/index.html` before overwriting it, because it currently holds the Step 2 smoke evidence. Suggested name: `smoke.html`. 2. Copy the combined run report to: `out/reports/FR-04-personal-profile/html-report/index.html` 3. Update `html-report/README.md` so: `index.html` = combined FR-04 run; `smoke.html`, `batch-a.html`, `batch-b.html`, `batch-c.html` remain historical evidence 4. Ensure bug reports/issue references remain accurate after this change. After the run: 1. Update … automation/report.md with combined results: total executions; pass/fail by case/project; confirmation that known failures remain known root causes; browser coverage count stated honestly: UI cases provide browser evidence; API-only cases are matrix uniformity 2. Update … bug-reports/report.md if combined-run evidence should be cited. 3. Update `out/README.md` FR-04 summary if that file is ready to receive feature-level results. 4. Run: `npm run verify:report`; verifier on copied `index.html`; verifier on `smoke.html`, `batch-a.html`, `batch-b.html`, `batch-c.html`; `npx tsc --noEmit`; `git log --oneline -- '*.spec.ts'` 5. Update AI Audit … 6. Commit combined FR-04 output as non-qualifying. Stop after the combined FR-04 output commit. Do not start Step 4 yet." | Combined run of all 16 FR-04 cases: **48 executions, 21 passed / 27 failed, zero timeouts**, 1.5 min — matching the batch-derived expectation exactly (7 pass / 9 fail per project). All 27 failures collapse to the three already-filed root causes; **no fourth appeared**.<br><br>Produced: §16 of the FR-04 automation report (totals, per-case table, defect mapping, honest browser-coverage split, assertion-pattern inventory, Step 3 exit-criteria table); a rewritten `out/README.md` with the feature-level summary; `index.html` replaced by the combined report with the Step 2 artefact preserved as `smoke.html`; `html-report/README.md` rewritten. | **INCOMPLETE** | Two issues, both about **reporting honesty** rather than test logic — the code needed no change at all:<br><br>**(a) Overwriting `index.html` would have silently invalidated filed evidence.** GitHub issue #1 cites `html-report/index.html` as `BUG-04-101`'s evidence, and that path held the Step 2 smoke report. Replacing it in place would leave a filed bug pointing at a *different artefact than the one it was filed from* — the reader could not tell the citation had shifted underneath them.<br><br>**(b) The browser-run count was the obvious place to inflate a number.** 48 executions across three "browser" projects reads like 48 browser runs, and HW04 §6 asks for ≥9. But 10 of the 16 cases never request Playwright's `page` fixture, so **no browser launches** for 30 of those executions. Reporting 48 would overstate multi-browser evidence by 2.7×. | **(a)** Preserved the original bit-for-bit as **`smoke.html`** before overwriting, then documented the swap in three places: `html-report/README.md` (a dedicated "note for anyone following a link" section), the bug report's evidence list (now citing `smoke.html` as the filing-time artefact *and* `index.html` for the combined evidence), and this row. The link is not broken — the combined report contains the same failing case plus the rest of FR-04, so it is strictly *more* evidence — but the exact cited artefact remains inspectable rather than being quietly replaced.<br><br>**(b)** Counted only the **6 UI-path cases × 3 browsers = 18 genuine browser executions**, and stated the exclusion explicitly in the automation report §16.4, `out/README.md` §3 (with a footnote), and `html-report/README.md`. The run timings corroborate the split (UI 1.9–23.5 s vs API 93–273 ms). FR-04 clears HW04 §6's ≥9 bar on its 18 real runs alone, so the honest number was never in tension with the requirement — which is precisely why inflating it would have been gratuitous.<br><br>**No `.spec.ts` was modified** — no `fix:` commit. The zero-flake result across 48 concurrent profile-mutating executions also retro-validates the Step 1 isolation decision (architecture §3.2). |
| **Artifact #8 — Agent Skill `test-automation-design` (HW04 §7)**<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 20:40–21:20 09/08/2026<br><br>Prompt (verbatim, complete): "Review current repo state first, then proceed with Step 4 only: extract the reusable `test-automation-design` skill from the completed FR-04 work. Before Step 4, fix resume-state housekeeping in `docs/implementation-plan/implementation_plan.md`: Mark Step 3 / FR-04 full pilot as complete. Record combined output commit `12c5585`. Record final FR-04 result: 16/16 cases automated; 48 executions; 21 passed / 27 failed; 18 genuine browser executions; 3 confirmed defects: BUG-04-101, BUG-04-102, BUG-04-103. Keep `.spec.ts` ledger as 5 total / 4 freezes: freezes: `e6cd87f`, `8053add`, `5af1749`, `6942a0a`; optional R: `9e6a8bb`. Change NEXT ACTION to Step 4 skill extraction. Remove the duplicated 'Carry forward' paragraph around the old Step 3 next-action block. Commit this docs-only housekeeping separately as non-qualifying if changed. Then execute Step 4 only: 1. Read the completed FR-04 artifacts: `out/reports/FR-04-personal-profile/automation/report.md`; `automation/data/fr-04-profile.json`; FR-04 specs under `automation/tests/fr-04-profile/`; `docs/implementation-plan/automation-architecture.md`; current `docs/implementation-plan/implementation_plan.md`. 2. Extract methodology notes from what actually worked in FR-04: freeze-before-run discipline; data externalization; oracle traceability to spec / accepted assumptions; human review before execution; real-defect gate; no assertion weakening; batch sizing and qualifying commit ledger; UI-first with justified API-assist; test-scoped `freshUser` for profile/state mutation; report-stamp verification; honest browser-coverage counting; bug handling: update existing issue for same root cause, create new issue only for distinct root cause; AI audit row discipline. 3. Generate `.claude/skills/test-automation-design/SKILL.md` using the existing `generate-skill` approach. Do not hand-write an overfit project script. The skill must be repo-agnostic and feature-agnostic. It must not mention FR-04, EShop, HW04, localhost, ports, 23127179, project paths, or specific bug IDs. It may mention generic concepts like 'feature spec,' 'test basis,' 'automation framework,' 'HTML report,' and 'frozen test case.' 4. Validate the skill: Run it conceptually or in a dry-run prompt against the FR-04 inputs. Confirm it would reproduce an equivalent workflow: select cases, externalize data, generate batches, review before freeze, run after freeze, classify failures, report evidence. Do not overwrite existing FR-04 deliverables. 5. Run coupling smell-test on the generated skill: grep for: `FR-0`, `EShop`, `eshop-sut`, `localhost`, `5173`, `5174`, `out/reports`, `Playwright`-specific paths, `23127179`, `HW04`; zero hits required, except if a term appears only inside a clearly generic warning list because the plan explicitly requires this grep. Prefer zero hits. 6. Mirror the skill to `.codex/skills/test-automation-design/SKILL.md` so Claude/Codex copies are byte-identical. If project sync tooling exists, use it. Otherwise copy the generated skill exactly and verify byte identity. 7. Update AI Audit with a new row for Step 4 skill extraction. Include the prompt or a clear complete artifact reference. Mark verdict based on review. Update totals. 8. Commit Step 4 as non-qualifying: commit message: `Step 4: extract test-automation-design skill`. This step should touch skill files and necessary docs/audit only. It must not touch `.spec.ts`. Stop after Step 4 commit. Do not start FR-08 yet." | **A 7-phase, framework-agnostic skill** (`SKILL.md`, 25.7 KB, sha256 `2bb4b512…bada1cc5`) generated through the existing `generate-skill` template. Phases: (1) select cases + map mechanisms + size batches, (2) externalize data with its oracle, (3) generate one batch, (4) **review before freezing**, (5) freeze then execute, (6) classify every failure, (7) report evidence honestly. Its centrepiece is a table of **8 recurring generated-code failure modes** generalised from the 23 review findings actually recorded across FR-04's four batches — headed by "inventing a refusal mechanism", which recurred in three separate batches (findings 7, 15, 21). Also carries the real-defect gate ("did it fail *at an assertion*?"), the root-cause distinctness test ("would fixing one fix the other?"), and the honest-coverage rule. Mirrored byte-identically to `.codex/`; smell-test 0 hits. | **INCOMPLETE** | Two problems, one in the skill and one in how the deliverable would have been delivered:<br><br>**(a) A gate in the skill was wrong, and validation caught it.** Phase 4.3's "no invented-oracle assertion" gate was first specified as a grep for the operator alone (`toBeNull` / `toBe('')` / `toBe(400)`). Run literally against the finished FR-04 suite it produced **3 false positives** — all three assert on a *captured request object* ("was a request issued?"), which is the legitimate capture-then-assert pattern the skill itself prescribes in Step 3.6, not a claim about how invalid input must be refused. A skill shipping this gate would have sent a future run chasing correct code.<br><br>**(b) The skill would not have reached a grader.** The plan specifies `.claude/skills/…` as the output location, but `.claude/` and `.codex/` are *separate git repos* recorded in the submission repo as gitlinks (mode `160000`) with **no `.gitmodules`**. Cloning the submission repo yields two empty directories — so the entire HW04 §7 deliverable (10 pts) would have been invisible, while every local check still passed. | **(a)** Rewrote the gate to match the operator **together with what it is applied to**, and added an explicit instruction to read every hit before acting on it. Re-run scoped, it returns **zero**, confirming no genuine invented oracle exists in the suite. This is the skill's own Phase 5.4 rule turned on itself — *a check that cannot fail for the right reason is worse than no check* — and it is the strongest evidence that the validation step was real rather than ceremonial: it changed the artefact.<br><br>**(b)** Added `out/agent-skill/SKILL.md`, a byte-identical third copy inside the submission folder (all three verified equal by `cmp` + `sha256sum`), plus `out/agent-skill/README.md` recording provenance, the gitlink reason, the phase map, and the full validation trace. `out/README.md`'s deliverable index and self-assessment row updated. The config copies remain the source of truth; the note states that any change must be re-mirrored.<br><br>**Validation performed, not just claimed:** each of the 7 phases was traced against what FR-04 actually did, and Phase 4.3's static gates were executed literally against the finished suite (16 cases parsed with per-batch counts 1/4/5/6, all carrying `expectedSource`; typecheck exit 0; 48 tests discovered; 0 pre-seeded-account references; 0 inline data literals; 30 oracle citations). No FR-04 deliverable was modified. |
| **Artifact #9 — FR-08 Checkout case design (Step 5.1)**<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 21:30–22:10 09/08/2026<br><br>Prompt (verbatim, complete): "First, push the completed Step 4 work, then start Step 5.1 design only. Push work: 1. Push the parent submission repo. 2. Push `.claude`. 3. Push `.codex`. 4. Show the resulting `git status --short --branch` for all three. If any push fails, stop and report it. Do not start FR-08 until the push state is clear. Then begin Step 5.1 only: FR-08 Checkout case design. Do not write automation specs yet. Do not run tests. Before designing, fix one stale plan issue: In `docs/implementation-plan/implementation_plan.md`, remove the FR-04-specific Step 5 carry-forward notes about `freshUser` and profile-form `getByLabel`. Replace them with a neutral note: Step 5 must re-read FR-08 UI/API and derive fixtures/selectors from checkout behavior, not from FR-04 profile behavior. Commit this docs-only fix separately if changed. For FR-08 design: 1. Read: `.claude/skills/test-automation-design/SKILL.md`; `docs/implementation-plan/implementation_plan.md` Step 5; `eshop-sut/README.md` FR-08 only; `eshop-sut/api_specification.md` checkout/order/cart endpoints; HW02 FR-08 reports under `references/hw2/.../FR-08-checkout/`; existing automation architecture/docs from Step 1. 2. Confirm the FR-08 test basis: R1: only logged-in user can checkout; R2: total computed automatically from cart and not directly user-editable; R3: checkout UI displays full ordered-product list; R4: backend recomputes total; client-sent `total_amount` is not accepted; R5: successful checkout clears the cart. Explicitly exclude FR-07 cart quantity/stock, FR-09 coupon, FR-10 order status, and unspecific shipping validation as counted FR-08 cases. 3. Build the FR-08 case set: Include the 4 reusable HW02 FR-08 cases. Design at least 8 new cases so total is at least 12. Every case must map to R1–R5. Prioritize R2 and R3 because the plan says they had zero HW02 coverage. Mark any setup touching login/cart as setup only, not counted FR-08 coverage. 4. For every case, record: ID; source: HW02 or new; requirement ref R1–R5; mechanism: UI / APIRequestContext / request interception; input/precondition; expected outcome; expectedSource; reason for mechanism; planned batch A/B/C. 5. Apply the `test-automation-design` skill Phase 1 only: select cases; map mechanisms; flag convergence/duplicates; size batches; list non-automatable cases, if any. 6. Write the design into: `out/reports/FR-08-checkout/automation/report.md`; update `docs/implementation-plan/implementation_plan.md` Step 5 status/NEXT ACTION to 'FR-08 Batch A freeze'; optionally create a design-only data draft only if it is clearly marked not-yet-frozen; otherwise leave data creation for the Batch A freeze commit. 7. Update AI Audit with a new row for the FR-08 design session if Claude generated the case design. 8. Commit this as non-qualifying docs/design work. Do not touch `.spec.ts`. Do not run FR-08 tests. Do not start Batch A automation yet." | **A 15-case FR-08 design** (4 reused from HW02 + **11 new**, minimum 12), each mapped to R1–R5 with mechanism, precondition, expected outcome, `expectedSource`, mechanism rationale and planned batch. Coverage: R1 = 3, **R2 = 3**, **R3 = 3**, R4 = 4, R5 = 2 — the two requirements with **zero** HW02 coverage (R2, R3) each receive three new cases. Batches: **A** = 6 UI (R2/R3), **B** = 6 API (R1/R4), **C** = 3 cross-surface (R5 + R1-UI). Four convergence relationships declared; **zero** non-automatable cases. Two new accepted assumptions recorded (A-08-1, A-08-2, both MED) for the two cases not backed by a direct spec citation. | **INCOMPLETE** | Two design errors, both caught by reading the SUT rather than by reasoning from the spec alone — and the second one invalidates a decision frozen back in Step 1:<br><br>**(a) R5 was initially treated as one requirement with one observation.** It is not. The SUT has **two disconnected carts**: a server-side `userCarts[userId]` in-memory object written only by `POST /api/cart`, and an unpersisted client-side React state in `CartContext.jsx` read by `Checkout.jsx`. The UI never calls `POST /api/cart`, and `POST /api/checkout` reads *neither*. HW02's `TC-08-EP-004` observes only the server cart, so a UI checkout's effect on the cart the customer actually sees was entirely uncovered — and treating a new UI case as a duplicate of the HW02 one would have dropped it.<br><br>**(b) `automation-architecture.md` §3.1, frozen in Step 1, uses FR-08's forged `total_amount` as its worked example of a case requiring request interception, on the stated premise that "the client computes the total; there is no field to type a forged value into." **That premise is false for this SUT** — `Checkout.jsx:14` holds the total in editable state and lines 93–102 render it as a plain `<input type="number">`. Designing to the frozen architecture note would have produced an interception-based case for a defect any customer can trigger by typing. | **(a)** Split R5 across two surfaces: `TC-08-EP-004` (server cart, API) and the new `TC-08-N07-UI` (client cart, UI), with the two-cart architecture documented in §2.1 of the design and the non-duplication argued explicitly — different stores, different code paths, and a disagreement between them would itself be a finding. The same reading produced a hard constraint now recorded for every UI batch: the client cart is unpersisted React state, so cases must seed **and** assert within one page session — a `page.reload()` would silently destroy the cart and read as flakiness.<br><br>**(b)** Corrected the mechanism map — `TC-08-N03-UI` is a **UI** case and **no** FR-08 case uses interception. Recorded the correction in §2.2 of the design rather than silently routing around it, and scoped it precisely: §3.1's *policy* (UI-first, interception as a justified exception) stands; only its illustrative example was wrong. This also changes the **claim** a defect there would support — from "an attacker crafting requests can forge the total" to "any customer can type a different number into the form", which is a materially different severity argument.<br><br>**Also corrected:** the plan still carried FR-04's `freshUser` / `getByLabel` conclusions into Step 5. Fixture scope and selector strategy are **per-feature** conclusions derived from a feature's own markup and mutation profile; checkout mutates cart and order state across two surfaces. Replaced with the method to apply (skill Phases 3.1 / 3.5) instead of the FR-04 answers — committed separately as `5f2c3f1`.<br><br>**Scope discipline held:** the checkout page renders an FR-09 coupon panel; no case asserts coupon behaviour. Login and add-to-cart are marked **setup, not coverage**. No `.spec.ts` written, no data file created (a design-stage data file would be a half-frozen artefact), nothing run. |
| **Artifact #10 — FR-08 Batch A: spec generation, execution and defect classification**<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 22:20 09/08/2026 – 13:30 10/08/2026<br><br>Prompt (verbatim, complete — three linked sessions): **(1) Freeze:** "Review current repo state first, then proceed with FR-08 Batch A freeze only. Do not run Batch A. Do not start Batch B. Batch A scope: Six UI cases from `out/reports/FR-08-checkout/automation/report.md`: `TC-08-N01-UI` through `TC-08-N06-UI`; Requirements: R2 and R3 only; This is the first FR-08 qualifying freeze commit. … Important constraints: 1. Externalize Batch A data to `automation/data/fr-08-checkout.json`. No inline test-data arrays/objects in specs. Every case must carry `expectedSource`. Mark A-08-1 and A-08-2 as MED-confidence assumptions, not direct spec citations. 2. Generate only Batch A specs under `automation/tests/fr-08-checkout/`. Import from `../../fixtures/base`. Derive fixture scope and selectors from checkout/cart behavior, not from FR-04. Seed the client cart through the UI and assert within the same page session. Do not use `page.reload()` between cart seeding and assertion. Do not use `page.route()` for Batch A. Register a dialog handler where needed because `Checkout.jsx` can call `alert()`. 3. Human-review before freezing. … 4. Static gates before commit … 5. Record pre-run prediction for each Batch A case before running … 6. Commit before any run: `freeze: FR-08 specs batch A`. Stop immediately after the freeze commit." **(2) Pre-run corrections:** "Review current repo state first. Do not run FR-08 Batch A yet. Before running, fix two pre-run review findings in the frozen Batch A spec. These are test-quality defects found after freeze but before first execution, so they must be corrected before any run. 1. `TC-08-N01-UI` and `TC-08-N03-UI` still require the total input to exist. The spec comments correctly say a compliant implementation could omit the editable total field. But the current code calls `totalInput(page).isEditable()`, which would fail if the input is absent. Fix so absence of the direct total input is treated as compliant evidence, not a harness error. Record annotations for: total input present/absent; editable true/false if present; whether an edit was attempted. If the input is absent or non-editable, skip `fill()` and still assert the outcome … Do not assert `disabled`, `readonly`, or any specific prevention mechanism. 2. `TC-08-N05-UI` quantity assertion is too weak. `line.toContain(String(product.quantity))` can pass because another digit in the product name or amount matches the quantity. Replace it with a standalone numeric-token assertion … Keep the oracle scoped to A-08-1 …" **(3) Run:** "Review current repo state first. Before running Batch A: 1. Push the parent repo … Then run FR-08 Batch A only: Command: `cd automation && npx playwright test tests/fr-08-checkout`; No retries … Expected pre-run prediction: N01 FAIL, N02 PASS, N03 FAIL, N04 PASS, N05 PASS MED oracle via A-08-1, N06 PASS MED oracle via A-08-2; Expected total: 12 pass / 6 fail over 18 executions. After the run: 1. Compare actual vs prediction per case and browser. 2. Apply the real-defect gate: Assertion failure → possible product defect; Timeout/setup/load error → test-side defect, not product. 3. If a test-side defect appears: Fix only waits/setup/mechanism. Do not change expected values. Commit separately as `fix: FR-08 Batch A post-run corrections`. Re-run Batch A and show both before/after. 4. If product defects appear: Do not weaken assertions. Corroborate independently outside Playwright where practical. Group by root cause using 'would fixing one fix the other?' Create or update FR-08 bug reports and GitHub issues. N03 likely maps to HW02 BUG-08-001, but create/update HW04 evidence rather than relying only on HW02. Decide whether N01 is the same root cause as N03 or a distinct frontend affordance defect after evidence, not before. …" | Six UI cases externalized to `data/fr-08-checkout.json` (rules, not literals) plus `utils/checkout.ts` and `tests/fr-08-checkout/checkout-page.spec.ts`, frozen at **`9b0ab82`** before any run. **14 pre-freeze review findings (24–37)**, **2 post-freeze/pre-run findings (38–39)**, and **2 post-run test-side corrections**. Final result: **12 passed / 6 failed over 18 executions**, prediction **6/6 correct**, two confirmed defects filed as issues [#4](https://github.com/BuhDuy256/automation-testing-hw04/issues/4) and [#5](https://github.com/BuhDuy256/automation-testing-hw04/issues/5). | **INCOMPLETE** | Four problems, of which the two most serious were **failure modes the generated tests could not have detected about themselves**:<br><br>**(a) The tests required the defect to be present in order to run.** `TC-08-N01-UI`/`N03-UI` called `isEditable()` directly, which *waits* for the element — so a compliant implementation that simply **omits** the editable total field would have been reported as a harness error rather than a pass. A test that cannot pass against a correct implementation is measuring the implementation, not the requirement.<br><br>**(b) `TC-08-N05-UI` could pass without the quantity being rendered at all.** `toContain('3')` is a substring test and the seeded product is named *"Tai nghe AirPods Pro **2**"* — with a quantity of 2 the assertion passed on the product name alone.<br><br>**(c) Four of run 1's ten failures were harness timeouts, not defects.** Had they been taken at face value, `N02`, `N04` and `N05` would have become **three fabricated bug reports** for browser-specific FR-08 defects that do not exist.<br><br>**(d) A generated data file would have hardcoded the expected total.** R2 states a *relationship*, not a number; a literal would encode the SUT's seed data as if it were the spec. | **(a)** `attemptDirectTotalEdit()` probes presence with `count()` first and edits only when possible; the outcome assertion runs on every path, and presence/editability/attempted are recorded as **annotations** (evidence, not oracle). Still no assertion on `disabled`/`readonly`.<br><br>**(b)** The product name is stripped, then the quantity must match as a **standalone numeric token** with lookarounds excluding digits and thousands separators. **Proven to fail for the right reason before being trusted** — verified that the old form false-passed on the name and the new form does not.<br><br>**(c)** Classified by the real-defect gate (*did it fail at an assertion?*) and fixed at the cause across two separate commits, **neither touching an expected value**: `test.slow()` scoped to this file (measured: `N04` takes 4.1 s isolated on firefox but exceeded 30 s in the batch — all 18 executions here are browser-heavy, where FR-04's batches were two-thirds API-path), then **retry-until-navigated** for the client-side route changes. That second cause was diagnosed from the timeout's **page snapshot**, which showed the app still on the storefront with the last product button focused — a click swallowed during a React re-render. The six assertion failures were **identical across all six runs**.<br><br>**(d)** Totals are computed at runtime as Σ(catalogue price × quantity) from `GET /api/products`, an independent source; the data file carries the rule.<br><br>**Root-cause grouping decided by evidence, not prediction.** §9 left open whether `N01` and `N03` share a cause. Independent corroboration reproduced the backend defect **with no browser at all**, proving it survives any frontend fix; conversely a recomputing backend would still leave the total user-editable. Filed as **two** distinct defects rather than one. **`BUG-08-102` is HW04's own UI-path evidence** for HW02's `BUG-08-001`, which upgrades the severity argument from *"an attacker who can craft requests"* to *"any customer who can type"*.<br><br>**Reported honestly rather than tidied:** firefox showed intermittent Playwright *driver* instability (a teardown protocol crash) in 2 of 6 runs. It never reaches an assertion. Recorded as a known environment flake instead of re-running until a clean pair appeared. |
| **Artifact #11 — FR-08 Batch B: API spec generation, execution and defect consolidation**<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 13:40–15:10 10/08/2026<br><br>Prompt (verbatim, complete — two linked sessions): **(1) Freeze:** "…Then proceed with FR-08 Batch B freeze only. Do not run Batch B. Do not start Batch C. Batch B scope: `TC-08-001`, `TC-08-EP-002`, `TC-08-EP-003`, `TC-08-N08-API`, `TC-08-N09-API`, `TC-08-N10-API`. Requirements: R1: only logged-in users can check out; R4: backend recomputes total and does not accept client-sent `total_amount`. Use the `test-automation-design` skill Phases 2–4 only: 1. Append/externalize Batch B data in `automation/data/fr-08-checkout.json`. Preserve Batch A unchanged. Every Batch B case must include `expectedSource`. Data carries oracle and payload shape, not observed behavior. Parse the JSON immediately after editing. 2. Generate Batch B API spec(s)… 3. Use APIRequestContext only. Do not request `page`. Do not count these executions as browser coverage later. Matrix execution is uniformity only. 4. Fixture/state rules: For R4 cases that persist orders, use test-scoped users or unique per-case markers so each test can identify its own order. Do not assert global order counts. Seed the server cart through `POST /api/cart` only where the case needs a server-cart precondition. Compute expected total from independent catalogue/product data, not from the checkout result. Use unique `shipping_address` markers if needed… 5. Oracle discipline: For R4, assert persisted order total equals the cart-derived total, not the client-sent value. For omitted `total_amount`, the expected result is still cart-derived total per README line 107. For no-auth / invalid-JWT R1 cases, assert checkout does not succeed and no order is created for the test marker. Assert status codes only if `api_specification.md` explicitly defines them; otherwise record status as annotation/evidence. 6. Human-review before freezing… Check especially: invented status-code oracle; relying on global counts; confusing server cart with client cart; using Batch A UI helpers by accident; inline data literals; missing identity/order marker; counting API matrix as browser coverage; weakening known BUG-08-102 assertions. 7. Static gates before commit… 8. Record pre-run prediction… 9. Commit before any run: `freeze: FR-08 specs batch B`. Stop immediately after the Batch B freeze commit." **(2) Run:** "Before running Batch B, fix one reporting precision issue… clarify the 'Batch A preserved unchanged' gate. The accurate statement is: Batch A data entries were preserved when Batch B was appended; Batch A spec was changed only for throwing type-narrowing accessors after the shared data union widened; No Batch A oracle, expected value, or assertion changed… Then run FR-08 Batch B only: `cd automation && npx playwright test tests/fr-08-checkout/checkout-api.spec.ts`; No retries. Do not start Batch C. Expected prediction: `TC-08-001` FAIL, `TC-08-EP-002` PASS, `TC-08-EP-003` PASS, `TC-08-N08-API` FAIL, `TC-08-N09-API` FAIL, `TC-08-N10-API` FAIL; Expected total: 6 pass / 12 fail over 18 executions. All four R4 failures should update existing `BUG-08-102` / issue #5 unless evidence shows a distinct root cause. After the run: 1. Compare actual vs prediction per case and project. 2. Apply the real-defect gate… 3. If test-side defects appear: fix only waits/setup/mechanism; do not change expected values… 4. If product defects appear: do not weaken assertions; corroborate `BUG-08-102` independently where useful; update bug report and GitHub issue #5 with Batch B evidence; do not create duplicate issues for the same absent backend recomputation root cause. Reporting: … State explicitly that Batch B contributes 0 browser runs; its 18 executions are matrix uniformity only…" | `tests/fr-08-checkout/checkout-api.spec.ts` plus six Batch B cases appended to `data/fr-08-checkout.json`, frozen at **`286f437`** before any run. **11 pre-freeze review findings (40–50)** and a pre-run prediction. Executed first-invocation clean: **6 passed / 12 failed over 18 executions in 7.5 s**, prediction **6/6 correct**, zero timeouts, **no post-run correction required**. All 12 failures collapse into the existing `BUG-08-102`; issue [#5](https://github.com/BuhDuy256/automation-testing-hw04/issues/5) was **updated, not duplicated**. | **INCOMPLETE** | Three problems, the first of which damaged an artefact that was already frozen and already reported on:<br><br>**(a) Appending Batch B broke Batch A's already-frozen spec — at the type level.** `input` is inferred as a union across every case in the shared data file, so the moment Batch B introduced cases carrying `serverCart` instead of `products`, five call sites in `checkout-page.spec.ts` stopped compiling. That file was frozen at `9b0ab82`, already run, and is the evidentiary basis of two filed defects. **This is the third distinct failure mode caused by appending to a shared data file**, after FR-04's two missing-comma incidents.<br><br>**(b) A status-code oracle was the obvious generation, and no source for one exists.** For the R1 cases the natural assertion is `expect(status).toBe(401)`, and the middleware really does return 401/403 — but `api_specification.md` documents **no status code for any endpoint**, so asserting the observed value would derive the oracle from the implementation. Fourth consecutive batch in which this failure mode appeared.<br><br>**(c) 'No order was created' invited a global-count assertion**, which is meaningless when three projects run in parallel and every previous run's orders persist.<br><br>A fourth risk was latent rather than committed: `GET /api/admin/orders` — needed as a verification channel because an auth-boundary test has no identity to search with — turns out to apply **no role check**. The temptation was to bank that as a third FR-08 defect. | **(a)** Added throwing narrowing accessors (`seededProducts` in Batch A; `serverCartOf` / `totalAmountSpecOf` in Batch B) rather than casting the `undefined` away, so a missing key stays loud. **No Batch A oracle, expected value or assertion was altered** — verified by diffing against the freeze — so Batch A's 12/6 result and both filed defects still stand. The report's gate row was then corrected from the false claim 'Batch A preserved unchanged' to a three-row table separating data entries (unchanged), spec (mechanically changed), and oracles/expectations (unchanged).<br><br>**(b)** No status assertion anywhere in the file; status is pushed as an annotation so it appears as evidence. Same reframing HW02 recorded as assumption A6.<br><br>**(c)** Each test stamps a unique `shipping_address` marker and asserts **zero orders carry it** — identity, not arithmetic, and correct under any amount of parallelism.<br><br>**The unguarded admin endpoint was documented as out of scope, not claimed.** It belongs to the admin surface, not to FR-08's R1–R5; no assertion touches it. Same treatment as Batch A's `ProductDetail` click-count defect.<br><br>**Result quality.** Batch B needed **no post-run correction at all** — a direct contrast with Batch A's two, which locates Batch A's trouble in *driving a UI under parallel load* rather than in anything about FR-08. The four R4 failures were corroborated outside Playwright and sharpened the diagnosis twice: `N08` (field omitted → **NULL** persisted) proves there is **no recomputation at all**, not merely weak validation; `N10` (`0` → `0` persisted) rules out a falsy guard. That changes the recommended fix from 'tighten a validation rule' to 'add a computation that does not exist'. Grouping by *would fixing one fix the other?* gave **yes in every direction**, so issue #5 was updated rather than duplicated. Browser coverage was reported as **0 for Batch B** — its 18 executions launch no browser — keeping FR-08 at 18 rather than inflating to 36. |
| **Artifact #12 — FR-08 Batch C: cross-surface spec, execution and two-defect separation**<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 15:20–17:10 10/08/2026<br><br>Prompt (verbatim, complete — two linked sessions): **(1) Freeze:** "…start FR-08 Batch C freeze only. Do not run Batch C yet. Use `test-automation-design` for the freeze/review/static-gate phases only. Batch C cases: 1. `TC-08-EP-004` — R5 API: after successful checkout, the server cart is cleared. 2. `TC-08-N07-UI` — R5 UI: after successful UI checkout, the client-visible cart is cleared. 3. `TC-08-N11-UI` — R1 UI: anonymous checkout never succeeds. Important constraints: Judge server cart and client cart separately. Do not merge them into one defect before evidence. Do not use `page.reload()` after seeding a client cart; React cart state is in memory. For `N11-UI`, if the original path cannot be implemented without destroying client cart state, record that as a review finding and use an in-app flow that still proves 'anonymous checkout never succeeds'. Register dialog handlers before actions that may trigger `alert()`. Do not count API-only executions as browser coverage. Preserve Batch A and Batch B data entries unchanged unless a review finding justifies a mechanical correction. Static gates before freeze: `fr-08-checkout.json` parses and contains 15 cases total: A=6, B=6, C=3; `npx tsc --noEmit` passes; Playwright discovery finds 9 Batch C tests; No Batch C test-results exist before the freeze; No seeded `test@eshop.com`; No inline oracle data in the spec; No invented status-code/global-count assertions unless directly supported by the spec. Record a pre-run prediction before committing… Freeze commit must be: `freeze: FR-08 specs batch C`. Stop after the freeze. Do not execute Batch C." **(2) Corrections + run:** "…I checked the freeze and found three fixes needed first: 1. Fix stale plan/report status… 2. Fix the FR-08 report's stale N11 case table… 3. Fix `TC-08-N11-UI` before execution: In checkout-cart-and-access.spec.ts, after direct anonymous confirm, the test currently waits for the checkout button to be visible again. That is a mechanism/harness assertion. If the product wrongly succeeds, the success screen replaces the button and the test may fail before the actual oracle. Replace it with capture-then-assert: capture the /api/checkout response as an optional value; do not require the button to reappear; assert the success heading count is 0; assert no captured checkout response is 2xx. Do not assert a specific status code or redirect target. Commit the spec change separately as `fix: FR-08 Batch C pre-run review corrections`… After that, run Batch C exactly once… Classify failures through the real-defect gate, decide whether server-cart and client-cart R5 failures are one root cause or two only after evidence, update bug reports/issues, verify the HTML report stamp, commit output, and stop before the combined FR-08 run." | `tests/fr-08-checkout/checkout-cart-and-access.spec.ts` plus three Batch C cases appended to `data/fr-08-checkout.json`, frozen at **`050a468`** with corrections at **`3e77f44`**, both before any run. **9 pre-freeze review findings (51–59)** and **3 pre-run corrections (60, 61a, 61b)**. Executed once: **3 passed / 6 failed over 9 executions**, prediction **3/3 correct**, zero timeouts, no post-run correction. Two **distinct** defects filed — [#6](https://github.com/BuhDuy256/automation-testing-hw04/issues/6) and [#7](https://github.com/BuhDuy256/automation-testing-hw04/issues/7). | **INCOMPLETE** | Four problems, the first two of which would each have destroyed the value of the batch:<br><br>**(a) `TC-08-N11-UI` was unimplementable as designed.** The design said *no session; navigate directly to `/checkout`; seed cart; confirm.* That sequence cannot exist: seeding the client cart requires the storefront, and reaching `/checkout` anonymously requires a page load, which remounts `CartProvider` and **empties the cart** — while the in-app route is closed because `Cart.jsx:12` redirects anonymous users. The case would have tested an **empty-cart** checkout while claiming to test an **anonymous** one.<br><br>**(b) The test could not reach its own oracle if the product misbehaved — three times over.** After the anonymous confirm it waited for the checkout button to still be visible; but `Checkout.jsx:68-76` **replaces the whole form** with the success screen on success, so the one defect the case exists to catch would have surfaced as *'the checkout button never settled'*. Two further instances: the settle check admitted only the compliant outcomes, and requiring the checkout form to render would have failed a **more** compliant implementation that guarded the route.<br><br>**(c) The two carts invited being merged into one case or one defect** — the obvious simplification, and the one that would have hidden half the problem.<br><br>**(d) A grep-based classification briefly mis-called a product defect as a harness failure.** Scanning failure output for `setup failed` matched a string inside `TC-08-N07-UI`'s file that came from a different message; the actual `Error:` line was the oracle. | **(a)** Restructured into **two in-app observations** that together prove more than the original: the anonymous user does not reach checkout from the cart, and a direct visit produces no completed checkout. Recorded as finding 51 rather than silently redesigned. A second consequence was recorded as finding 52: **'no order created' cannot use a marker here**, because `Checkout.jsx` sends only `{items, total_amount, coupon_id}` and never `shipping_address` — so the observable became the app's own completion signal plus the checkout response, never a global order count.<br><br>**(b)** All three replaced with capture-then-assert / presence-probing, so the oracle is reachable on every path. **This is the third distinct appearance of the same failure mode** (after Batch A's finding 38 and Batch B's type-union break), which is why it is now written up as a general rule: *a test must not require the defect to be present in order to run*.<br><br>**(c)** Kept as two cases with separate stores, separate assertions and separate failure messages, each naming the other as independently judged — and the root-cause decision was deferred to **after** the run. That deferral paid off: the evidence showed **two** defects, not one. Clearing `userCarts[userId]` cannot empty the browser's React state, and calling `clearCart()` cannot empty the server cart. Had they been merged, the report would have claimed a single cart bug and a backend-only fix could have shipped while the customer still saw items they had already paid for. `BUG-08-104` has **no HW02 precedent at all** — HW02's FR-08 pass was entirely API-path and never observed the store the customer actually sees.<br><br>**(d)** Caught by reading the actual failure rather than trusting the scan, and recorded in report §18.3 as a method note: a grep over failure output is not a classification.<br><br>**Evidence quality.** `BUG-08-104`'s root cause is unusually clean — `clearCart` is **defined, exposed and destructured, with zero call sites anywhere in the codebase**. `BUG-08-103` was corroborated with no browser (1 cart line before and after a successful checkout). Browser coverage was counted case by case: **6 of Batch C's 9** executions, because `TC-08-EP-004` launches none. |
| **Artifact #13 — Combined FR-08 run: feature-level report and coverage correction**<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 17:20–18:40 10/08/2026<br><br>Prompt (verbatim, complete): "Before the combined FR-08 run, fix two stale documentation lines only: 1. In out/reports/FR-08-checkout/automation/report.md top status: It still says only Batches A+B executed and Batch C frozen/not run. Update it to say all three batches executed: 45 executions, 21 passed / 24 failed, 4 defects (#4–#7), combined run pending. Update section mapping to include Batch C execution §18. 2. In out/reports/FR-08-checkout/html-report/README.md: Heading currently says 'Browser coverage — 18 of 36'. Correct it to '24 of 45'. Fix the stale sentence saying Batch B inflation is 18→36. Current honest total is 24 browser runs; counting Batch B as browser coverage would inflate to 42, and counting every matrix execution would inflate to 45. Commit this as docs-only / non-qualifying, then push all local commits including f7458c9. Confirm `## main...origin/main` is clean. Then run the combined FR-08 suite exactly once: `cd automation && npx playwright test tests/fr-08-checkout`. Do not edit `.spec.ts` before the run. Expected from batch evidence: 45 executions total; 21 passed / 24 failed; 24 genuine browser runs; no new root cause expected. If the combined result differs, treat it as an interaction/flakiness signal and classify through the real-defect gate. Do not weaken assertions. After the run: Copy the combined HTML report to `out/reports/FR-08-checkout/html-report/index.html`. Preserve `batch-a.html`, `batch-b.html`, and `batch-c.html`. Run the report stamp verifier on `index.html` and the batch files. Update FR-08 automation report, html-report README, bug report if needed, out/README, implementation plan, and AI audit. Commit output as non-qualifying. Stop before FR-15." | Combined run of all 15 FR-08 cases: **45 executions, 21 passed / 24 failed, 3.6 min** — matching the batch-derived expectation exactly (7 pass / 8 fail per project). All 24 failures are assertion failures collapsing to the **four** already-filed root causes; **no fifth appeared**. Produced §19 of the FR-08 report (totals, per-case table, defect mapping, honest browser split, requirement verdict, assertion-pattern inventory, Step 5 exit criteria), the combined `index.html` alongside the three preserved batch reports, and the corrected status and browser-coverage headings. | **INCOMPLETE** | Two stale figures, both of which would have **understated or misstated the honesty argument** the reports are built on:<br><br>**(a) The report's top status still claimed only Batches A+B had run**, with Batch C 'frozen, not yet run' — stale by a full batch, two defects and one execution cycle. A reader arriving at the main report would have taken the feature as two-thirds finished.<br><br>**(b) The browser-coverage heading still read '18 of 36'**, and its explanatory sentence still described the inflation risk as '18 → 36'. Both were correct only before Batch C existed. The true figures are **24 of 45** — and, more importantly, the *inflation* framing was wrong in a way that undersold the point: counting Batch B as browser coverage gives **42**, and counting every matrix execution gives **45**, not 36. | **(a)** Status rewritten to 'FR-08 complete' with the combined totals, and the section map extended through §19.<br><br>**(b)** Heading corrected to **24 of 45**, and the inflation sentence rewritten to name both wrong numbers explicitly (42 and 45) with the observation that *both inflated figures would still be true statements about how many executions occurred* — which is precisely why the distinction has to be stated rather than left to the reader.<br><br>**Combined-run result required no correction of any kind.** 45 executions, 21 / 24, zero timeouts, zero setup failures, zero driver crashes, and **no `.spec.ts` was touched**, so there is no `fix:` commit against it. The firefox driver instability recorded during Batch A did not recur. Running three batches together — mixing UI and API paths that concurrently mutate carts, orders and sessions against one SQLite database — changed nothing, which retro-validates the per-test isolation and the per-test markers.<br><br>**Honest reporting held at the feature level.** FR-08 is recorded as **24** browser runs, not 45: 8 of its 15 cases are UI-path, and the other 7 launch no browser. Counting all executions would have inflated the figure by 87% while remaining literally 'true'. |
| **Artifact #14 — FR-15 Product CRUD case selection and design (Step 6.1)**<br><br>Tool: **Claude Code (Claude Opus 5)**<br>Time: 18:50–19:40 10/08/2026<br><br>Prompt (verbatim, complete): "Before starting FR-15, fix one small shared-doc gap: In `out/README.md` §5 'Test cases not automated', add FR-08: None for FR-08 — all 15 selected/designed cases were automated and executed. Mention that the two assumption-grounded R3 cases (`A-08-1`, `A-08-2`) passed but remain weaker evidence, not non-automated gaps. Commit as docs-only / non-qualifying. Push current local commits including `2b3de71`. Confirm `## main...origin/main` is clean. Then start Step 6.1 only: FR-15 Product Management CRUD case selection/design. Do not create data files or `.spec.ts` yet. Do not run anything. Use `test-automation-design` Phase 1 only. Read these authoritative inputs: `eshop-sut/README.md` FR-15 lines 191–198; `eshop-sut/README.md` FR-12 lines 176–179 for admin-only access; `eshop-sut/api_specification.md` product/admin-product sections; HW02 FR-15 reports (domain-testing, boundary-value-analysis, bug-reports). Selection requirements: HW02 has 20 frozen FR-15 cases; select at least 12. Prefer a 3-batch plan of 4–6 cases each, because FR-15's first freeze clears the §12 minimum and the plan expects three FR-15 freeze commits. Cover all FR-15 requirement areas: admin can add/view/edit/delete products; name required and max 255; price required and > 0; category required and existing; editing one product does not change other products; admin-only access for product write APIs. Map each selected case to UI / APIRequestContext with a reason. Products are global state: every product name must be unique, assertions must target only rows/products created by the test, and no assertion may use total product count. Do not depend on seeded products except as read-only catalogue/category references. If using admin UI (`frontend-admin`, port 5174), re-derive selectors from that app; do not inherit storefront helpers. For forbidden access cases, APIRequestContext is expected because the UI should not expose those operations to unauthorized users. For invalid inputs, assert outcome only: 'not persisted as invalid value' unless the spec/API explicitly defines a status or error contract. Outputs: Create/update `out/reports/FR-15-product-crud/automation/report.md` with: selected cases table; excluded HW02 cases with reasons; mechanism mapping; batch plan; non-automatable cases, if any; risks to carry into Batch A. Update `docs/implementation-plan/implementation_plan.md` NEXT ACTION to FR-15 Batch A freeze after selection. Update AI audit with this selection/design artifact. Commit docs as non-qualifying. Stop after Step 6.1 selection/design. Do not start Batch A freeze." | An **18-case FR-15 selection** — 16 of HW02's 20 plus **2 newly designed** — each mapped to requirement (P1–P6), mechanism, precondition, expected outcome, `expectedSource`, mechanism rationale and batch. Four HW02 cases excluded with stated reasons. Coverage: P1 = 3, P2 = 4, P3 = 3, P4 = 2, P5 = 2, P6 = 4. Batches of **6 / 6 / 6**; **zero** non-automatable cases; **8 risks** recorded for Batch A. | **INCOMPLETE** | Three problems, two of which are traps that would only have surfaced mid-implementation:<br><br>**(a) HW02's set has a hole the requirement list exposes.** It contains **no positive delete case at all** — `TC-15-EP-009` covers `DELETE` *without* authorisation, but nothing covers *'Admin có thể … Xóa sản phẩm'* succeeding. Half of P1 was untested, and selecting purely from HW02 would have reported full coverage of an area that had none.<br><br>**(b) `seedSession()` would have silently failed on the admin app.** Every FR-04 and FR-08 UI test uses it, and it writes `localStorage.token` — but `frontend-admin/src/App.jsx:7` reads **`adminToken`**. Reusing the helper would have left the admin app logged out, and the failure would have presented as a routing or rendering problem rather than an authentication one.<br><br>**(c) The 'never depend on seeded data' rule cannot hold here, and the tempting workaround is worse than the exception.** `POST /api/register` inserts a user with no role, so an admin cannot be created through the API. The available alternative — registering a user and self-escalating via `BUG-04-103` — would make the whole FR-15 suite **depend on a filed security defect remaining unfixed**. | **(a)** Designed **two** new cases rather than papering over it: `TC-15-N01-API` (admin deletes a product they created; it is no longer retrievable) and `TC-15-N02-UI` (admin adds a product through the panel). The second also fixes a coverage-shape problem — **19 of HW02's 20 cases are API-path**, so automating only those would leave the admin *interface* almost untested for a requirement written about what an admin can do. Both are labelled **new** in the case table so provenance stays visible.<br><br>**(b)** Recorded as risk 2 with the exact line reference, and Batch A is instructed to write an admin-specific session helper. The inverse trap was recorded alongside it: the admin form **does** expose `placeholder` attributes, so `getByPlaceholder` works there — the *opposite* of the storefront profile form. Carrying either conclusion across features would be wrong in a different direction each time.<br><br>**(c)** Taken as a **stated exception** rather than silently or by exploit: the seeded admin is used **as a credential only**, nothing is asserted about that account, and every product created remains uniquely named and privately owned by its test. Written into the report so a reviewer sees the reasoning instead of an unexplained dependency.<br><br>**Exclusions justified, and of two different kinds — an initial draft wrongly called all four 'redundancy'.** `EP-004` and `EP-005` are genuine **convergence**: same input class, same mechanism and same assertion as their BVA twins, so keeping both would double-count one piece of evidence. `BVA-001` and `BVA-008` are **selection tradeoffs**: each is a legitimate boundary HW02 designed on purpose — the lower valid bound of the name-length set, and the last valid member of the category enum — omitted only to keep the selected set at 18 while P1–P6 stay covered. Both carry an explicit reinstate condition (`BVA-001` if the name constraint later needs the full boundary set; `BVA-008` if enum completeness becomes the focus), and either can be added back without disturbing the batch structure. Describing them as redundant would have misrepresented HW02's design. **None** of the four was dropped for being hard to automate.<br><br>**Global-state rule made concrete.** FR-15 is the first feature where architecture §3.2's shared-state clause actually binds: products belong to no user. Unique names, assertions scoped to test-created rows, and **no total-count assertion** are recorded as a hard rule for the batch, with the same reasoning that produced FR-08's per-test order markers. |

---

## 4. Summary of AI Accuracy

Aggregate the verdicts from Section 3 and complete the table below.

> _Running totals — updated as artifacts are added. Final figures are computed at submission._

| Metric                                   | Count | Percentage |
| ---------------------------------------- | ----- | ---------- |
| **Total AI-generated artifacts audited** | 14    | 100%       |
| **VALID (correct, accepted as-is)**      | 0     | 0%         |
| **INVALID (wrong; rejected)**            | 0     | 0%         |
| **INCOMPLETE (acceptable after edits)**  | 14    | 100%       |

---

## 5. Conclusion — When should AI be used (or not)?

Write 80–150 words describing patterns you observed. Where did AI shine? Where did AI fail? What is your recommendation for using AI in this kind of work in the future?

_(Write your conclusion here.)_

---

## 6. Mandatory Disclosure (paste verbatim)

> "[Test cases / script / dataset / report] was initially generated by [AI tool name]; I reviewed and modified [section X], added [edge cases Y, Z]; [section W] was written entirely by me. The detailed AI Audit Report is attached as Appendix A. I confirm I did not use AI to generate any artifact listed in the prohibited category."

### Signature

| Field                       | Value                                         |
| --------------------------- | --------------------------------------------- |
| **Student name (printed):** | Nguyen Bao Duy                                |
| **Student ID:**             | 23127179                                      |
| **Class / Cohort:**         | 23KTPM2                                       |
| **Course:**                 | CS423 / CSC13003 – Software Testing           |
| **Instructor:**             | Lam Quang Vu, Truong Phuoc Loc, Ho Tuan Thanh |
| **Date:**                   | 09/08/2026                                    |
| **Signature:**              | Nguyen Bao Duy                                |

---

## References

- Kharbach, M. (2026). _AI Use Policy Templates for Higher Education._ CC BY-NC-SA 4.0.
- ISTQB Foundation Level Syllabus (latest version).
- Hardman, P. (2025). _A Post-AI Learning Taxonomy._
- Fuster Rabella, M. (2025). OECD Education Working Paper No. 338.
- Perkins, M., Roe, J., & Furze, L. (2025). _AI Assessment Scale._
- Anthropic (2025). _Building reliable AI test agents_ — engineering blog.
- DeepEval & Promptfoo documentation — testing frameworks for LLM systems.
