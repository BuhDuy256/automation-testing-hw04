# ACT-GEN-01 Prompt — FR-04 schema and request shape

Generate candidates for JSON shape, field types, extra fields, and response/read-back schema. Use documented fields and explicitly record where the specification does not define requiredness, nullability, extra-field handling, or response status/schema.
