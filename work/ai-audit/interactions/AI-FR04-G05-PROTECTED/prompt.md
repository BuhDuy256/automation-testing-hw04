# ACT-GEN-01 Prompt — FR-04 protected fields

Generate candidates for client attempts to submit `email` and `role` while updating the profile. Apply FR-04 and SEC-06, distinguish ignore/reject behavior from the invariant that protected fields must not change, and do not call the known implementation observation a bug.
