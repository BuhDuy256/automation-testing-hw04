# ACT-GEN-01 Prompt — FR-04 authentication

Generate focused candidates for missing, malformed, invalid, expired, and valid JWT handling on `PUT /api/users/me`. Use SEC-02 and the verified extract. Do not invent exact status codes where the source is silent.
