# ACT-GEN-01 Prompt — FR-04 text fields

Generate candidates for `name` and `shipping_address` using ordinary, Unicode, empty, null, wrong-type, long, and HTML-like values. Separate contract-supported behavior from exploratory behavior; never require rejection when the specification is silent.
